<%= content_for :title, "#{@team.name}" %>
<%= content_for :show_tabs, "team_show" %>

<div class="navbar" style="margin-bottom:0;"> 
  <div class="navbar-inner">
  	<%= render 'options' %>
  </div>
</div>


<div id="tabGroupContent" class="tab-content">
	<div class="tab-pane fade in active" id="team_details">
	  <div class="content_box"><%= render 'details' %></div>
	</div>
	<div class="tab-pane fade" id="team_wall">
	  <div class="content_box"><%= render 'wall' %></div>
	</div>
	<div class="tab-pane fade" id="team_study_plan">
	  <div class="content_box"><%= render 'study_plan' %></div>
	</div>
</div>

<div class="horizontal_line"></div>

<div class="content_box">
<%= link_to "<i class='icon-arrow-left icon-small'></i> Back".html_safe, teams_path %>
</div>